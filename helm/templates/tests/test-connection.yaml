apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "avalond.fullname" . }}-test-connection"
  labels:
    {{- include "avalond.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "avalond.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
